/**
 * Re-export: wrap app shell (or a subtree) to scope the slap shake to that region.
 * @see SlapAnimationProvider in context
 */
export {
  SlapAnimationProvider as SlapAnimationWrapper,
  SlapAnimationProvider,
} from "../../context/SlapAnimationContext.jsx";
